{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm turbo run build --filter=@crm/web"
  },
  "deploy": {
    "startCommand": "pnpm --filter @crm/web start"
  }
}
